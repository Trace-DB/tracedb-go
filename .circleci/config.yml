version: 2.1

jobs:
  tracedb-go:
    docker:
      - image: cimg/go:1.24
    steps:
      - checkout
      - run:
          name: Repository policy checks
          command: |
            test -f SECURITY.md
            test -f README.md

workflows:
  ci:
    jobs:
      - tracedb-go:
          name: circleci/tracedb-go
